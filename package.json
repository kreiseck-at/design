{
  "name": "@kreiseck/design-workspace",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node tools/build.mjs",
    "build:npm": "pnpm -C packages/npm build",
    "check": "node tools/build.mjs --check",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p packages/npm/tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
