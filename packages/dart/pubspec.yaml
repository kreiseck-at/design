name: kreiseck_design
description: Design tokens, theme and widgets shared by all Kreiseck surfaces.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.1
  flutter: ">=3.44.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
